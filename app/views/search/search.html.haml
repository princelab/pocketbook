#search
  %h2 Search
  = form_tag(search_path, :method => "get") do
    = label_tag(:organism_name, "Search by organism name:")
    = text_field_tag(:q)
    = submit_tag("Search")
  = render :partial => "layouts/raw_data_sets_table" unless @raw_data_sets.nil?
